# أوامر تشغيل الموقع / Site Run Commands

## 📋 المتطلبات / Requirements
- Node.js (v18 أو أحدث)
- PostgreSQL (للباك إند)
- npm أو yarn

---

## 🚀 طريقة التشغيل السريعة / Quick Start

### 1️⃣ تشغيل الباك إند (Backend) - Terminal 1

```bash
# الانتقال لمجلد الباك إند
cd backend

# تثبيت المكتبات (أول مرة فقط)
npm install

# إنشاء ملف البيئة (أول مرة فقط)
# إذا كان هناك .env.example، انسخه:
# copy .env.example .env
# ثم عدّل .env بإعدادات قاعدة البيانات

# تشغيل قاعدة البيانات (PostgreSQL)
# تأكد أن PostgreSQL يعمل على جهازك

# تشغيل المايجريشن (أول مرة فقط)
npm run migrate

# إضافة بيانات تجريبية (أول مرة فقط - اختياري)
npm run seed

# تشغيل السيرفر
npm run dev
```

الباك إند سيعمل على: **http://localhost:3001**

---

### 2️⃣ تشغيل الفرونت إند (Frontend) - Terminal 2

```bash
# العودة للمجلد الرئيسي
cd ..

# تثبيت المكتبات (أول مرة فقط)
npm install

# تشغيل الموقع
npm run dev
```

الفرونت إند سيعمل على: **http://localhost:5000**

---

## 📝 الأوامر الكاملة / Complete Commands

### الباك إند / Backend Commands

```bash
cd backend

# التثبيت
npm install

# التطوير (مع إعادة التشغيل التلقائي)
npm run dev

# الإنتاج
npm start

# المايجريشن
npm run migrate

# إضافة بيانات تجريبية
npm run seed
```

### الفرونت إند / Frontend Commands

```bash
# من المجلد الرئيسي

# التثبيت
npm install

# التطوير
npm run dev

# البناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start
```

---

## 🔧 إعداد ملف البيئة / Environment Setup

### للباك إند (backend/.env):

```env
DATABASE_URL=postgresql://user:password@localhost:5432/database_name
JWT_SECRET=your-secret-key-here
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5000
```

### للفرونت إند (.env.local):

```env
NEXT_PUBLIC_API_URL=http://localhost:3001/api
```

---

## 🐳 تشغيل مع Docker (اختياري)

### الباك إند فقط:

```bash
cd backend
docker-compose up -d
```

---

## ✅ التحقق من التشغيل / Verification

### 1. الباك إند:
افتح المتصفح على: http://localhost:3001/api/health
يجب أن ترى: `{"status":"ok"}`

### 2. الفرونت إند:
افتح المتصفح على: http://localhost:5000
يجب أن ترى صفحة الموقع

---

## 🔑 بيانات الدخول الافتراضية / Default Login

بعد تشغيل `npm run seed` في الباك إند:

- **البريد الإلكتروني**: `admin@alwameed.com`
- **كلمة المرور**: `123`

---

## ⚠️ ملاحظات مهمة / Important Notes

1. **يجب تشغيل الباك إند قبل الفرونت إند**
2. **تأكد أن PostgreSQL يعمل**
3. **شغّل المايجريشن أول مرة فقط**
4. **استخدم terminal منفصل لكل سيرفر**

---

## 🛠️ حل المشاكل / Troubleshooting

### البورت 3001 أو 5000 مستخدم:
```bash
# Windows PowerShell
netstat -ano | findstr :3001
taskkill /PID <رقم_البروسيس> /F
```

### خطأ في قاعدة البيانات:
- تأكد أن PostgreSQL يعمل
- تحقق من إعدادات `.env`
- تأكد من وجود قاعدة البيانات

### خطأ في المكتبات:
```bash
# احذف node_modules وأعد التثبيت
rm -rf node_modules
npm install
```

